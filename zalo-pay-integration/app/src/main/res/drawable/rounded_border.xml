<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/white"/> <!-- Màu nền -->
    <stroke android:width="2dp" android:color="#CCCACA"/> <!-- Viền -->
    <corners android:radius="12dp"/> <!-- Bo góc -->
    <padding android:left="8dp" android:right="8dp" android:top="4dp" android:bottom="4dp"/>
</shape>
